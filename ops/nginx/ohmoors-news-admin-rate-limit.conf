# Global rate-limit zone for the protected news admin area.
# Install this file into /etc/nginx/conf.d/ so it is loaded from the http {} context.

limit_req_zone $binary_remote_addr zone=ohmoors_news_admin:10m rate=10r/m;
