# Cache static assets for one hour. events.json is handled separately.
location ~* \.(?:css|js|mjs|ico|gif|jpe?g|png|svg|webp|avif|pdf)$ {
    expires 1h;
    add_header Cache-Control "public, max-age=3600";
    try_files $uri =404;
}
