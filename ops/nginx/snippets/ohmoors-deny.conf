# Hide operational and editor artifacts from public access.
location ~ /\.(?!well-known/) {
    deny all;
}

location ~* \.bak(?:$|[.-]) {
    deny all;
}

location ~* (?:~|\.sw[op]|\.orig|\.rej|\.old|\.save)$ {
    deny all;
}
